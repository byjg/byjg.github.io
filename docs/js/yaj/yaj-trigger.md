---
sidebar_position: 28
---

# yo(selector).trigger(event)

Trigger a custom event defined previously by [yo(selector).bind()](yaj-bind).

See the example:

```javascript
yo('.myClass').trigger('some-user-event');
```